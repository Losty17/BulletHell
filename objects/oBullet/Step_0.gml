/// @description Insert description here
// You can write your code in this editor
direction += offset

if (min_speed != undefined && speed + acceleration >= min_speed) &&
   (max_speed != undefined && speed + acceleration <= max_speed)
{
	speed += acceleration	
}
